<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature android:name="android.hardware.camera" />

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".TomSnackApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TomSnacks"
        tools:targetApi="31">
        <activity
            android:name=".ui.report.ReportActivity"
            android:exported="false" />
        <activity
            android:name=".ui.cashier.LoadingActivity"
            android:exported="false" />
        <activity
            android:name=".ui.cashier.DetailCashierActivity"
            android:exported="false" />
        <activity
            android:name=".ui.members.AddMemberActivity"
            android:exported="false" />
        <activity
            android:name=".ui.members.MembersActivity"
            android:exported="false" />
        <activity
            android:name=".ui.cashier.CashierActivity"
            android:exported="false" />
        <activity
            android:name=".ui.inventory.InventoryActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>